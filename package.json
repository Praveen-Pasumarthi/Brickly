{
  "name": "brickly",
  "version": "0.1.0",
  "private": true,
  "description": "Brickly mobile game packaged for Android and iOS with Capacitor.",
  "scripts": {
    "build": "node scripts/build-mobile-assets.mjs",
    "cap:sync": "npm run build && npx cap sync",
    "cap:add:android": "npm run build && npx cap add android",
    "cap:add:ios": "npm run build && npx cap add ios",
    "android": "npm run build && npx cap sync android && npx cap open android",
    "android:debug:windows": "npm run build && npx cap sync android && cd android && set \"JAVA_HOME=C:\\Program Files\\Android\\Android Studio\\jbr\" && set \"PATH=%JAVA_HOME%\\bin;%PATH%\" && gradlew.bat assembleDebug",
    "ios": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor-community/text-to-speech": "^8.0.0",
    "@capacitor-firebase/authentication": "^8.2.0",
    "@capacitor-firebase/firestore": "^8.2.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.0.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.0.0",
    "firebase": "^12.14.0",
    "jimp": "^1.6.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
